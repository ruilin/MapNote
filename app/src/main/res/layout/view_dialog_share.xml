<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="20dp"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/dialog_share_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        android:src="@drawable/nav_head_bg"/>
    <EditText
        android:id="@+id/dialog_share_ed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:background="@color/white"
        android:textSize="14sp"
        android:text="我正在用旅行印记APP，生成出自己的旅行记录，来看看我走过哪些地方！"
        android:gravity="left|top"
        android:maxLength="500"
        android:padding="10dp"
        android:lines="2" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        android:orientation="horizontal">
        <Button
            android:id="@+id/dialog_share_cancel"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:background="@drawable/selector_button_gray"
            android:textColor="@color/textGray4"
            android:textSize="16sp"
            android:text="取消"
            />
        <Button
            android:id="@+id/dialog_share_ok"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:background="@drawable/selector_button"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:text="分享"
            />
    </LinearLayout>
</LinearLayout>